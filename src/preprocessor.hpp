#ifndef PREPROCESSOR_HPP__
#define PREPROCESSOR_HPP__

#include <vector>
#include <string>

namespace preprocessor {
  std::vector<std::string> process_all(int, char**);
};

#endif /* PREPROCESSOR_HPP__ */
